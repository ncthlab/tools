# 倉庫儲位地圖 HTML 系統需求規格

## 1. 系統目的

製作一套以 HTML 為主的「倉庫儲位地圖／庫存視覺化管理介面」。

主要功能：

* 匯入檔名以 `INVR` 開頭的 XLSX 庫存資料。
* 依據「區－級－行－列－層」解析儲位。
* 將實際倉庫儲位以網格地圖方式呈現。
* 顯示每個儲位目前的貨物及堆疊層數。
* 顯示倉庫儲位使用率。
* 提供即時搜尋。
* 搜尋結果可直接定位至地圖上的儲位。
* 透過動畫提供滑鼠操作、搜尋、定位、展開及資料更新等即時視覺回饋。
* 第一階段僅做「查看／分析」，不修改 ERP 或原始 XLSX。

---

# 2. 儲位編碼原則

基本儲位階層：

> **區－級－行－列－層**

| 層級 | 說明        |
| -- | --------- |
| 區  | 倉庫的大區域／象限 |
| 級  | 區內的次區域    |
| 行  | 儲位主要排列方向  |
| 列  | 同一行內的儲位位置 |
| 層  | 垂直堆疊高度    |

其中：

* `層 = 1` 為最底層。
* `層` 數字越高，代表越上方。
* 一個「實體儲位」可以包含多個層。
* 儲位統計以「實體儲位」計算，不以層數計算。

---

# 3. 倉庫四大區

倉庫共有四個主要區域：

* `01`
* `02`
* `03`
* `04`

實際地圖的排列方式，後續依提供的實際倉庫地圖／範例圖製作。

---

# 4. 級的編碼

目前已知的 `{級}`：

* `A`
* `B`
* `C`
* `D`
* `E`
* `NE`
* `NC`
* `NS`
* `S`

其中：

* `S` 為貨架／層架類型。
* `S` 在視覺地圖上需要旋轉 90° 呈現。
* 不可以單純依 `{級}` 代碼判斷方向。
* 實際方向必須依完整的 `{區級}` 組合判斷。

---

# 5. 側置 `{區級}` 定義

以下 `{區級}` 明確定義為「側置」：

* `01C`
* `02B`
* `02NE`
* `02NC`
* `03B`
* `04C`

也就是：

> **側置判斷以完整 `{區級}` 為準，而不是單純依 `{級}` 判斷。**

例如：

* `02B` → 側置
* `03B` → 側置

即使兩者 `{級}` 都是 `B`，仍需依 `{區級}` 判斷。

---

# 6. 側置方向

側置 `{區級}` 的行排列方向以 **Y 軸**為基準。

目前確認：

```text
01C
02B
02NE
02NC
03B
04C
```

皆採側置方向。

但是：

* Y 軸正方向／負方向
* 螢幕上到底是由上往下或由下往上
* 行號在左側或右側
* 側置儲位的實際排列方向

仍需依實際倉庫地圖範例最終確認。

---

# 7. 一般儲位方向

一般 X 軸排列的儲位：

* 行沿 X 軸遞增。
* 若行為 X 軸方向，通常列沿 Y 軸遞增。
* 實際畫面方向以實際倉庫地圖為最終依據。

系統不得單純假設：

> X 軸一定是畫面由左往右。

而應將：

* X 正方向
* X 負方向
* Y 正方向
* Y 負方向

設定為可由儲位結構設定控制。

---

# 8. S 貨架

`S` 為特殊貨架／層架。

基本資料編碼原則仍與其他儲位一致：

> 區－級－行－列－層

但在地圖上：

* 視覺上旋轉 90°。
* 原本水平排列的儲位，在 S 貨架上轉為垂直排列。
* 每一個實體儲位仍算一個儲位。
* 旋轉不影響儲位數量統計。
* 層數仍維持原始資料定義。
* S 的行號方向依實際倉庫地圖決定。

---

# 9. `{區級}` 視覺區域框架

每一個 `{區級}` 都必須是一個獨立的視覺區域。

例如：

```text
┌──────────────────────────────┐
│  01A                         │
│  ┌───┬───┬───┬───┬───┐       │
│  │   │   │   │   │   │       │
│  ├───┼───┼───┼───┼───┤       │
│  │   │   │   │   │   │       │
│  └───┴───┴───┴───┴───┘       │
└──────────────────────────────┘
```

## 9.1 區域框線

每一個 `{區級}`：

* 使用獨立外框。
* 每個 `{區級}` 使用不同辨識色。
* 相鄰 `{區級}` 的顏色必須容易區分。
* 避免過度鮮豔。
* 不使用大量高飽和霓虹色。

## 9.2 外框漸層光暈

每個 `{區級}` 外框：

* 使用與框線同色系的柔和漸層光暈。
* 光暈向外擴散。
* 透明度低。
* 擴散範圍小。
* 不得產生刺眼霓虹效果。
* 不得遮蓋儲位資料。

目的：

> 讓使用者一眼辨識不同 `{區級}` 的範圍，而不是作為裝飾。

## 9.3 Bookmark 區域標籤

每個 `{區級}` 左上角增加小型 Bookmark 標籤。

例如：

```text
┌───────┐
│ 01A   │
└───────┘
```

規則：

* 顯示完整 `{區級}`。
* 例如：

  * `01A`
  * `01B`
  * `01C`
  * `02B`
  * `02NE`
  * `02NC`
  * `03B`
  * `04C`
* 標籤顏色與該 `{區級}` 框線同色系。
* 標籤屬於 `{區級}` 容器。
* 不在每個儲位重複顯示 `{區級}`。

---

# 10. `{區級}` 互動動畫

系統需要具備互動動畫效果。

但動畫目的為：

> **提供操作回饋，而非純裝飾。**

## 滑鼠移入 `{區級}`

當滑鼠移入某個 `{區級}`：

* 外框稍微變亮。
* 光暈稍微增加。
* Bookmark 輕微放大／浮起。
* 該區域視覺上略微突出。
* 不可大幅移動。

## 點擊 `{區級}`

點擊後可以讓該 `{區級}`：

* 框線變亮。
* Bookmark 強調。
* 光暈增加。
* 其他 `{區級}` 輕微降低視覺權重。

再次點擊：

* 取消選取。

不得：

* 閃爍。
* 抖動。
* 大幅縮放。
* 造成地圖位移。

---

# 11. 主地圖

主畫面以倉庫網格地圖為核心。

每個實體儲位為一個 Grid Cell。

儲位 Cell 主要顯示：

* 儲位代碼。
* 目前堆疊層數。

例如：

```text
01A01-01
   3
```

代表該實體儲位目前有 3 層貨物。

---

# 12. 空儲位顯示

如果儲位沒有貨物：

```text
0
```

並以綠色表示可用／空置。

例如：

```text
┌─────────┐
│ 01A01-01│
│    0    │
└─────────┘
```

綠色狀態屬於「儲位狀態」。

不可被 `{區級}` 框線顏色取代。

也就是：

* `{區級}` → 外框／Bookmark 辨識色
* 儲位 → 綠色空位／貨物狀態色

兩者同時存在。

---

# 13. 儲位 Hover

滑鼠移到儲位時：

* 儲位框線稍微變亮。
* Cell 輕微放大。
* 增加陰影。
* 顯示該儲位完整貨物資料。
* Tooltip 或資訊卡顯示所有層。

動畫：

* 快速。
* 平滑。
* 移開後自然恢復。

不可：

* 閃爍。
* 大幅移動。
* 造成 Grid 重新排列。

---

# 14. 儲位貨物資料

INVR XLSX 匯入後，每筆貨物資料至少包含：

* `儲位`
* `品名`
* `品號`
* `批號`
* `期末庫存`
* `單位`
* `備註`
* `規格`

以上統稱：

> **貨物資料**

其中 `規格` 的實際來源欄位仍需以正式 INVR 範例確認。

---

# 15. 多層貨物顯示

同一實體儲位可能存在：

```text
層 3
層 2
層 1
```

顯示順序：

> `層 1` 在最底部，數字越高越上方。

Hover 儲位時，需可以查看該儲位所有層的貨物資料。

例如：

```text
儲位：01A01-01

第 3 層
品號：XXX
品名：XXX
批號：XXX
庫存：20 KG

第 2 層
品號：YYY
品名：YYY
批號：YYY
庫存：30 KG

第 1 層
品號：ZZZ
品名：ZZZ
批號：ZZZ
庫存：25 KG
```

---

# 16. 行號輔助標籤

每一個 `{級}` 內的每一個 `{行}`，在該行的第一個儲位位置附近增加小型行號。

目的：

> **讓使用者快速判斷目前正在查看第幾行。**

## 顯示規則

只顯示奇數行：

```text
1
3
5
7
9
11
13
...
```

偶數行不顯示。

例如：

```text
行 1   → 顯示 1
行 2   → 不顯示
行 3   → 顯示 3
行 4   → 不顯示
行 5   → 顯示 5
```

## 不使用前導 0

正確：

```text
1
3
5
7
9
11
```

不可：

```text
01
03
05
07
09
11
```

## 行號視覺樣式

行號屬於：

> **輔助資訊**

因此：

* 字體較小。
* 字重正常或偏細。
* 顏色保持足夠辨識度。
* 不可搶過儲位內容。
* 不可搶過搜尋結果。
* 不使用強烈動畫。

## 不影響 Grid

行號：

* 不得改變儲位 Cell 大小。
* 不得造成 Grid 對齊偏移。
* 不得改變儲位間距。
* 不得影響 Hover。
* 不得影響 Search。
* 不得影響點擊。

## 每個 `{級}` 重新計算

行號以 `{級}` 為單位重新開始：

```text
01A
1
3
5
7

01B
1
3
5
7
```

不可跨 `{級}` 連續編號。

## 側置及 S 貨架

側置 `{區級}` 與 `S` 貨架同樣採：

> 每隔一行顯示一次奇數行號。

但：

* 行號位於哪一側。
* 行號朝哪個方向排列。
* S 貨架旋轉後的行號位置。

需依實際倉庫地圖最終確認。

---

# 17. 行展開功能

每一行的第一個儲位之前增加展開按鈕。

初始：

```text
[＋]
```

點擊後：

```text
[－]
```

再次點擊：

```text
[＋]
```

---

# 18. 行展開內容

展開後：

> 在該行旁邊顯示側置詳細網格。

側置詳細網格：

* 以第一象限方向呈現。
* 每個位置代表實際貨物位置。
* 顯示貨物狀態。
* 支援 Hover。
* 支援搜尋 Highlight。
* 使用與主地圖相同的貨物資料。

---

# 19. 行展開動畫

點擊 `[＋]`：

* `＋` 平滑變成 `－`。
* 側置 Grid 平滑展開。
* 不讓整個地圖突然跳動。

關閉：

* `－` 平滑變回 `＋`。
* 側置 Grid 平滑收合。

動畫應：

* 短。
* 平順。
* 不阻塞操作。

---

# 20. INVR XLSX 匯入

使用者匯入：

> 檔名以 `INVR` 開頭的 `.xlsx`

例如：

```text
INVRxxxxx.xlsx
```

匯入後立即：

1. 讀取資料。
2. 解析儲位。
3. 建立儲位地圖。
4. 建立貨物索引。
5. 計算統計。
6. 更新搜尋資料。
7. 完成顯示。

---

# 21. 匯入動畫

匯入過程顯示簡潔狀態：

```text
讀取資料
↓
解析儲位
↓
建立地圖
↓
更新統計
↓
完成
```

不使用大型 Loading 畫面。

動畫必須簡潔，不影響使用者操作。

---

# 22. 資料與儲位結構分離

系統架構必須將：

> **儲位結構**

與：

> **貨物資料**

分開。

## 儲位結構負責

* 區
* 級
* 行
* 列
* 層
* X/Y 軸方向
* 側置判斷
* S 貨架方向
* 地圖位置
* `{區級}` 框線顏色
* Bookmark 顏色
* Glow
* 行號設定

## 貨物資料負責

* 儲位
* 品名
* 品號
* 批號
* 期末庫存
* 單位
* 備註
* 規格

---

# 23. 搜尋功能

畫面上方提供即時搜尋框。

搜尋：

> **不需要搜尋按鈕。**

輸入文字後即時搜尋。

支援模糊搜尋：

* 批號
* 品號
* 品名
* 規格
* 備註

---

# 24. 搜尋結果地圖效果

符合搜尋條件的儲位：

> 明顯 Highlight。

不符合的儲位：

> 降低透明度／視覺權重。

搜尋時：

* `{區級}` 外框仍保持。
* `{區級}` Bookmark 仍保持。
* `{區級}` Glow 不消失。
* 行號仍保持。
* 搜尋結果優先於一般區域效果。

---

# 25. 搜尋動畫

搜尋結果：

* 符合儲位平滑 Highlight。
* 不符合儲位平滑 Dim。
* 搜尋結果增加時淡入。
* 搜尋結果消失時淡出。

搜尋結果較少時：

* 可以使用非常輕微的 Pulse / 呼吸效果。

搜尋結果很多時：

* 自動降低或關閉 Pulse。
* 避免整個畫面閃爍。

---

# 26. 搜尋結果清單

右側顯示符合搜尋條件的貨物清單。

每筆結果至少包含：

* 品號
* 品名
* 批號
* 規格
* 儲位
* 庫存
* 單位
* 備註

---

# 27. 相同品號＋批號合併

如果：

```text
品號 + 批號
```

相同：

> 搜尋結果合併成一筆。

例如：

```text
01A01-01 → 20 KG
01A01-02 → 30 KG
```

如果兩筆：

```text
品號：ABC
批號：123
```

相同，則搜尋結果：

```text
品號：ABC
批號：123

儲位：
01A01-01
01A01-02

庫存：50 KG
```

目前規劃：

> 相同品號＋批號跨儲位時，庫存加總。

---

# 28. 搜尋結果定位

點擊右側搜尋結果：

1. 地圖自動找到對應儲位。
2. 平滑捲動／定位。
3. 目標儲位短暫強調。
4. 之後恢復正常狀態。

不可突然跳轉造成使用者迷失。

---

# 29. 統計資料

統計只計算：

> **實體儲位**

而不是層。

## 已使用儲位

有貨物的實體儲位數：

> `已使用儲位`

## 剩餘儲位

```text
剩餘儲位
=
全部可用實體儲位
-
已使用實體儲位
```

## 使用率

```text
使用率
=
已使用儲位
÷
全部可用實體儲位
× 100%
```

---

# 30. S 貨架統計

S 貨架：

> 每個實體位置算一個儲位。

不能因為 S 視覺旋轉而改變統計方式。

層數也不計入儲位數。

---

# 31. 統計動畫

匯入或搜尋資料更新後：

* 數字平滑變化。
* 使用率平滑更新。
* 可以使用 Ring Chart 或 Progress Bar。
* 不使用突然跳動的數字。

---

# 32. 主畫面配置

基本配置：

```text
┌─────────────────────────────────────────────────────┐
│ 倉庫儲位地圖     [匯入 INVR]     [即時搜尋]          │
├───────────────────────────────────────┬─────────────┤
│                                       │             │
│                                       │ 搜尋結果    │
│          倉庫 Grid Map                │             │
│                                       │ 貨物清單    │
│                                       │             │
├───────────────────────────────────────┴─────────────┤
│ 檔案資訊 / 匯入狀態 / 統計資訊                       │
└─────────────────────────────────────────────────────┘
```

左側：

> 倉庫地圖。

右側：

> 搜尋結果／貨物資料。

上方：

* 系統標題
* XLSX 匯入
* 即時搜尋
* 統計資訊

下方：

* 檔案資訊
* 資料狀態
* 系統狀態

---

# 33. 整體視覺風格

方向：

> 現代化倉庫管理 Dashboard。

特性：

* 清楚。
* 專業。
* 容易閱讀。
* 區域明確。
* 儲位狀態明確。
* 動畫低調。
* 不做過度科技感。

可使用：

* 圓角。
* 卡片。
* 柔和陰影。
* 淡色背景。
* 區域色框。
* 柔和 Glow。

但不能為了視覺效果犧牲：

* Grid 對齊。
* 資訊密度。
* 閱讀性。
* 操作速度。

---

# 34. 視覺層級

所有視覺效果的優先順序：

```text
1. 搜尋結果 Highlight
2. 點擊定位效果
3. 儲位 Hover
4. 行／區域 Hover
5. 儲位正常狀態
6. 行號輔助標籤
7. 區級框線／Bookmark
8. 區級 Glow
9. 背景／裝飾
```

其中搜尋 Highlight 優先權最高。

---

# 35. 動畫設計原則

動畫主要使用：

* CSS `transition`
* `transform`
* `opacity`
* `box-shadow`

避免：

* 高頻 JavaScript 動畫。
* 全部儲位持續動畫。
* 大量 DOM 重建。
* 大型 Canvas 不斷重新繪製。

---

# 36. 大型地圖效能

如果儲位數量很多：

* 不讓所有 Cell 持續動畫。
* Hover 才啟動局部動畫。
* 搜尋結果少時才使用 Pulse。
* 搜尋結果多時改成靜態 Highlight。
* `{區級}` Glow 使用低成本 CSS。
* 避免每次搜尋重新建立整張地圖。

---

# 37. 動畫不可影響功能

任何動畫：

* 不得阻止點擊。
* 不得延遲搜尋。
* 不得影響鍵盤操作。
* 不得造成 Grid 位置改變。
* 不得改變 Pointer Hit Testing。
* 不得造成地圖跳動。
* 不得干擾資料讀取。

---

# 38. 多狀態動畫優先權

當同一儲位同時符合多個狀態：

```text
搜尋結果
↓
點擊定位
↓
Cell Hover
↓
Row / Area Hover
↓
正常狀態
```

只呈現主要狀態。

避免多個動畫同時疊加造成：

* 閃爍
* 過亮
* 放大過度
* 視覺混亂

---

# 39. 第一階段不包含

第一階段只做：

> **庫存資料視覺化與查詢。**

不包含：

* ERP 庫存修改。
* INVR XLSX 編輯。
* 入庫作業。
* 出庫作業。
* 調撥作業。
* ERP 交易建立。
* ERP 資料庫直接連線。
* 使用者帳號權限。
* 儲位異動操作。
* 商品搬移。
* 儲位修改回寫。
* 實體貨物盤點。
* PDA。
* 手機掃碼。

---

# 40. 尚待提供／確認資料

在正式製作 HTML 前，需要取得：

## 40.1 完整儲位代碼

需要完整的實際儲位資料，例如：

```text
01A01-01
01A01-02
01A02-01
02B01-01
...
```

用於確認：

* 區
* 級
* 行
* 列
* 層
* 編碼規則
* 是否存在特殊格式。

---

## 40.2 實際倉庫地圖

需要提供實際倉庫地圖／範例圖片。

目的：

> **地圖結構與位置盡量 1:1 還原實際倉庫。**

視覺風格可以重新設計，但：

* 區域位置
* 行列方向
* 側置位置
* S 貨架方向
* 道路／空間關係

以實際地圖為準。

---

## 40.3 INVR XLSX 範例

需要實際的 INVR XLSX，以確認：

* 欄位名稱。
* 儲位格式。
* 規格欄位來源。
* 批號格式。
* 層的資料來源。
* 期末庫存格式。
* 0 庫存定義。

---

# 41. 尚待確認的規則

正式開發前需要確認：

### 儲位解析

* 完整儲位代碼格式。
* 行、列、層的切割方式。
* 是否每筆儲位資料都包含層。
* 層資料是否存在於儲位代碼中。

### 軸向

* X 正方向。
* X 負方向。
* Y 正方向。
* Y 負方向。

### 側置

已確認：

```text
01C
02B
02NE
02NC
03B
04C
```

皆為側置。

仍需確認：

* 實際 Y 軸排列方向。
* 畫面上的上下／左右方向。
* 行號位置。

### S 貨架

需要確認：

* 實際旋轉方向。
* 行號方向。
* 列方向。
* 地圖位置。

### 行號

已確認：

> 奇數行顯示行號，偶數行不顯示。

例如：

```text
1
3
5
7
9
11
```

仍需依實際地圖確認：

* 行號放左側或右側。
* 側置時的位置。
* S 貨架旋轉後的位置。

### 規格

確認 INVR 中 `規格` 的實際來源欄位。

### 0 庫存

確認：

> `期末庫存 = 0` 是否直接視為空儲位。

### 相同品號＋批號

目前規劃：

> 跨儲位合併，並加總庫存。

仍需確認特殊例外。

---

# 42. 正式開發前的最終確認順序

收到完整資料後，依以下順序確認：

```text
① 完整儲位代碼
        ↓
② 實際倉庫地圖
        ↓
③ INVR XLSX
        ↓
④ 確認儲位解析規則
        ↓
⑤ 確認 X / Y 方向
        ↓
⑥ 確認側置方向
        ↓
⑦ 確認 S 貨架方向
        ↓
⑧ 確認行號位置
        ↓
⑨ 確認庫存與搜尋規則
        ↓
⑩ 開始製作 HTML
```

在上述規則尚未確認前：

> **不直接進行 HTML 正式開發。**

---

# 43. 核心設計原則

整個系統的核心不是單純把 Excel 做成表格，而是：

> **將實際倉庫空間、儲位結構與目前庫存狀態，以可互動的地圖方式呈現。**

因此：

* `{區級}` 負責辨識空間區域。
* `{行}` 負責辨識主要排列方向。
* `{列}` 負責辨識實體儲位。
* `{層}` 負責辨識垂直堆疊。
* 行號負責快速定位行。
* 儲位 Cell 負責呈現目前庫存狀態。
* 搜尋負責快速找到貨物。
* 右側清單負責查看詳細資料。
* 動畫負責提供操作回饋。
* 統計負責呈現整體儲位使用狀況。

最重要原則：

> **實際倉庫結構優先於程式設計方便性。**
>
> **資料正確性優先於視覺效果。**
>
> **操作效率優先於裝飾動畫。**
